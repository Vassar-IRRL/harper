# Palm print files

Printable parts for HARPER’s palm / hand base. 

## Naming convention

Filenames will use `plm_{feature}.stl`.

| Token | Meaning |
|-------|---------|
| `plm_` | Palm |
| `stiff_frame` | Structural palm frame |
| `dxl_spool` | Dynamixel motor spool |

